{{ $maxLength := int 64 }}
{{ $serviceNameMaxLength := int (sub (sub $maxLength 1) (len .Values.environment)) }}
{{ $secretName := substr 0 $serviceNameMaxLength .Values.serviceName  }}

{{- if .Values.secrets -}}
---
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: sping-catalog-arm64
  namespace: {{ .Values.namespace }}
spec:
  provider: aws
  secretObjects:
  - secretName: spin/catalog/arm64 #{{ .Values.serviceName }}
    type: Opaque
    data:
    {{- range $.Values.secrets }}
    - key: {{ . }}
      objectName: {{ . }}
    {{- end }}
  parameters:
    objects: |
      - objectName: spin/catalog/arm64
        objectType: secretsmanager
        jmesPath:
          {{- range $.Values.secrets }}
          - path: {{ . }}
            objectAlias: {{ . }} 
          {{- end }}
{{- end }}
